{
  "name": "chaotools",
  "version": "0.1.0",
  "private": true,
  "description": "Chaotools - 你的工具，你做主",
  "scripts": {
    "dev": "pnpm -r --parallel dev",
    "build": "pnpm -r build",
    "lint": "pnpm -r --if-present lint && node scripts/validate-manifest.mjs",
    "test": "pnpm -r --if-present test && node scripts/validate-manifest.mjs",
    "clean": "pnpm -r clean"
  },
  "keywords": ["tools", "developer-tools", "monorepo"],
  "author": "Chaotools",
  "license": "MIT",
  "packageManager": "pnpm@10.33.0"
}
